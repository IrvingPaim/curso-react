const Challenge = () => {
    const x = 2
    const y = 10

    return (
        <div>
            <button onClick={() => console.log(x+y)}>Somar números</button>
        </div>
    )
}

export default Challenge